# Sample YAML configuration file.
# Copy to config.yaml or config.yml in the project root and adjust settings as needed.

# Sync schedule (daily at 17:55)
SYNC_CRON: "55 17 * * *"
SYNC_CRON_TIMEZONE: UTC

# Disable cron scheduling (daemon mode only; default: false)
DISABLE_CRON_SCHEDULING: false

# Port for web UI server (daemon mode only)
HTTP_PORT: 3000

# Directory to persist data files (mapping file, cookies, etc.; default: ./data)
DATA_DIR: "./data"

# Directory to cache Actual Budget downloads (default: ./data/budget)
BUDGET_DIR: "./data/budget"

# Path to JSON file to store pot-account mappings (relative to DATA_DIR; default: mapping.json)
MAPPING_FILE: "mapping.json"
